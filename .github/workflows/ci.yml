name: Test ruby/setup-ruby
on: [push]
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ windows ]
    runs-on: ${{ matrix.os }}-latest
    steps:
    - run: echo ${env:RUNNER_TOOL_CACHE}
    - run: pwd